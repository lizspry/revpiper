% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-joins.R
\name{rev_read_joins}
\alias{rev_read_joins}
\title{Read and validate the joins spec}
\usage{
rev_read_joins(path, dictionaries)
}
\arguments{
\item{path}{Path to the joins YAML file.}

\item{dictionaries}{Named list of \code{rev_dictionary} objects, as returned
by \code{\link[=rev_read_dictionaries]{rev_read_dictionaries()}}.}
}
\value{
A tibble with one row per declared join: \code{adds}, \code{left},
\code{right}, \code{keys_left} and \code{keys_right} (list columns), \code{relationship}
and \code{unmatched_ok} (both \code{NA} on observation appends, where they do
not apply).
}
\description{
Parses \code{specs/joins.yaml} against the loaded dictionaries. This is the
across-source validation step: data-free and composable, so each
dictionary still validates standalone and a table certification never
depends on it. A missing joins file is a valid single-table project and
returns the zero-row tibble.
}
