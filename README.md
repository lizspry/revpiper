# review-pipeline

revpiper: an R package turning systematic-review extraction data into manuscript-ready outputs via user-authored specifications.

Design documentation lives in `dev/superpowers/specs/` (arriving via PR).
